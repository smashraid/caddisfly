{
  "name": "@caddisfly/api",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "lint": "eslint \"src/**/*.ts\"",
    "check-types": "tsc --noEmit",
    "build": "tsup src/index.ts --format esm --out-dir dist"
  },
  "dependencies": {
    "@caddisfly/adapters": "workspace:^",
    "@caddisfly/core": "workspace:^",
    "@caddisfly/logger": "workspace:*",
    "express": "^5.2.1",
    "pino": "^10.3.1",
    "pino-http": "^11.0.0"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:^",
    "@types/express": "^5.0.6",
    "@types/node": "^26.1.0",
    "tsup": "^8.5.1",
    "tsx": "^4.23.0"
  }
}