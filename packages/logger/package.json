{
  "name": "@caddisfly/logger",
  "version": "0.1.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint \"src/**/*.ts\"",
    "check-types": "tsc --noEmit",
    "build": "tsup src/index.ts --format esm --dts --out-dir dist"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "tsup": "^8.5.1"
  }
}